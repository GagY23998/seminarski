@model IEnumerable<Artikl>
@inject Artikl artikl
<div class="box box-primary">
    <div class="box-body">
            @*<select asp-for="@artikl" asp-items="@(new SelectList(Model, "ArtiklID", "ImeArtikla"))" name="ArtiklID"></select>*@

            <table class="table table-bordered">
                <thead>
                    <tr class="text-center">
                        <td>Article Name</td>
                        <td>Price</td>
                        <td>Register Date</td>
                        <td>Article Quantity</td>
                        <td>Action</td>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="text-center">
                            <td>@item.ImeArtikla</td>
                            <td>@item.CijenaArtikla km</td>
                            <td>@item.VrijemePostavlanja</td>
                            <td>@item.NaStanju</td>
                            <td>
                            <a asp-controller="Admin" asp-action="DeleteArticle" asp-route-articleID="@item.ArtiklID">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
    </div>
</div>  