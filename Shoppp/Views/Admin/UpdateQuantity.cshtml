@model List<Artikl>
@{ Layout = "~/Views/Shared/_Layout.cshtml";}
@inject OnlineShopping.Services.IArtikliService service
<div class="KolicinaAzuriraj">
        <form>
            <select class="kategorijaChange" asp-items="@{new SelectList(service.GetCategories(),"KategorijaID","ImeKategorije","Nesto")}">
            </select>
            <div>
                <table >
                        
                    <tbody id="tabelaArtikala">
                            @*<tr>
                                <td><input class="form-control" asp-for="@item.ImeArtikla" /></td>
                                <td><input class="form-control" asp-for="@item.NaStanju" /></td>
                                <td><input class="form-control" asp-for="@item.CijenaArtikla" /></td>
                                <input type="hidden" asp-for="@item.KategorijaID" />
                            </tr>*@
                            <partial name="_ArtiklCategory" model="@Model"/>
                        
                    </tbody>
                </table>
            </div>
        </form>
</div>
<script>
    $(function () {
        $(".kategorijaChange").change(function () {
            $(".myitemRow  > .itemCategory").each((i, item)=>$(item).val() == $(".kategorijaChange").val() ?
                    $(item).parent().show() : $(item).parent().hide());
        });
    });


</script>
